import React from 'react';
import { View } from 'native-base';

// import { Container } from './styles';

const PageBody = props => {
  return <View px={17} flex={1} {...props} />;
};

export default PageBody;
